import React from 'react';
import {Screen} from '../../components/screen/screen';

export const EditOrAddMoneyCellScreen = (props) => {
    return (
        <Screen
            {...props}
            headerTitle = {`Add New MoneyCell`}
        />
    );
};
import {StyleSheet} from "react-native";

module.exports = StyleSheet.create({
    avatarContainer: {
        borderWidth : 3,
        borderColor: 'teal',
        width : 50,
        height: 50,
        justifyContent : 'center'
    }
});